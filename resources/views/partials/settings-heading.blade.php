<div class="relative mb-6 w-full">
    <p class="text-xs font-semibold uppercase tracking-[0.14em] text-zinc-500">{{ __('Settings') }}</p>
    <h1 class="mt-2 text-3xl font-extrabold tracking-tight text-[#174a34]">{{ __('Settings') }}</h1>
    <p class="mb-6 mt-2 text-base text-zinc-600">{{ __('Manage your profile and account settings') }}</p>
    <div class="h-px w-full bg-zinc-200"></div>
</div>
